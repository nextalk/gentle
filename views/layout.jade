include helper
doctype html
html(lang='en')
  head
    meta(charset='utf-8')
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    meta(name='description', content='')
    meta(name='author', content='')
    link(rel='shortcut icon', href='/favicon.ico')
    title= ( title ? title + " | " : "" ) + brand_name
    link(href='/static/css/base.min.css', rel='stylesheet')
    link(href='/static/css/bootstrap-theme.css', rel='stylesheet')
    block style
      style.
        body{padding-top: 70px;padding-bottom:30px;}
  body(role="document")
    .navbar.navbar-inverse.navbar-fixed-top(role="navigation")
      .container
        .navbar-header
          a.navbar-brand(href="/")= brand_name
        .navbar-collapse.collapse
          ul.nav.navbar-nav
            each t in tables
              li(class = t.name == table.name ? "active" : undefined)
                a(href="/" + t.name)= t.alias
          ul.nav.navbar-nav.navbar-right
            if username
             li 
               a(href="#")= username
             li
               a(href="/logout") Logout
            else
             li
               a(href="/login") Login

    .container(role="main")
      block content
    .container
      hr
      .footer
        p &copy; #{brand_name} 2014
    script(src='/static/js/base.min.js', type='text/javascript')
    script(src='/static/js/locales/bootstrap-datetimepicker.zh-CN.js', type='text/javascript')
